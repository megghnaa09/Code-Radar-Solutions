#include <stdio.h>
int main(){
    int age; char name[50]; char hobby[50];
    scanf("%s %d %s", %name, %age, &hobby);
    printf("Name: %s", name);
    printf("\nAge: %d",age);
    printf("\nHobby: %s", hobby);
    return 0;
}